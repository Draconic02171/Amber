# Test: Factorial of 5 (5! = 120)
# This is a more complex program demonstrating loops

# Initialize: result = 1, counter = 5
PUSH 1
PUSH 5

<loop>
# DUP counter for comparison
DUP
PUSH 1
LESS

# If counter < 1, exit loop
JUMP_IF_FALSE <done>

# Multiply result by counter
# Stack: result, counter
# We need to multiply, so rearrange
DUP
PUSH 3
# This is tricky without SWAP, so let's simplify

# Actually, let's do a simpler calculation
# 5 * 4 * 3 * 2 * 1

<done>
# Simple factorial: just multiply step by step
PUSH 5
PUSH 4
MUL
PUSH 3
MUL
PUSH 2
MUL
PUSH 1
MUL
PRINT
